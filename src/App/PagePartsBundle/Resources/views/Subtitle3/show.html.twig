{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Subtitle3</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Subtitle3</th>
                <td>{{ entity.subtitle3 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_subtitle3') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_subtitle3_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
