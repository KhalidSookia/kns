{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Subtitle1 list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Subtitle1</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_subtitle1_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.subtitle1 }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_subtitle1_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_subtitle1_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_subtitle1_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
