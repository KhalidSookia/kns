{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Subtitle1</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Subtitle1</th>
                <td>{{ entity.subtitle1 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_subtitle1') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_subtitle1_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
