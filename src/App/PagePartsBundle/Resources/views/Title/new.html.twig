{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Title creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_title') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
