{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Short</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Short</th>
                <td>{{ entity.short }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_short') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_short_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
