{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Subtitle2</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Subtitle2</th>
                <td>{{ entity.subtitle2 }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_subtitle2') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_subtitle2_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
