{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Subtitle2 list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Subtitle2</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_subtitle2_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.subtitle2 }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_subtitle2_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_subtitle2_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_subtitle2_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
